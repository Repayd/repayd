# REPAYD — ETHOnline 2026 Compliance Matrix

One table per track. **Every** qualification requirement and extra-scoring criterion from the official prize pages ([ethglobal.com/events/ethonline2026/prizes](https://ethglobal.com/events/ethonline2026/prizes)) is a row, verbatim. Evidence cells are grounded in real, independently-verified artifacts — the 14 Step-4 Arc-testnet transaction hashes (all re-fetched, status 0x1, see [`local/step4-final-report.md`](../../local/step4-final-report.md) and `.wsl/step4-receipt.json`), ERC-8004 registry readbacks (agentId **894341**, validation score **25 = COVERED**, feedback **−2500 @ 2dp**, verdict digest `0x1af03bc6a70b6309bd5c9ec92c7d78c1024e0d69c9ea5ea60faf828c958ed3f0`), the fresh-stack addresses in [`contracts/deployments/5042002.json`](../../contracts/deployments/5042002.json), and git commits. **No evidence is invented; every cred-gated item is marked.**

Status legend: **DONE** (verifiable now, in-repo or on-chain) · **IN-PROGRESS** (code exists, integration under construction) · **BLOCKED-ON-CRED** (work complete, credential needed to execute/record) · **NEEDS DECISION** (human call required).

---

## Track 1 — Arc: "Best Agentic Economy Application with Circle Agent Stack" ($3,500; $2,500 of the pool requires Arc **mainnet** deploy by **Sept 30, 2026**)

| # | Requirement (verbatim) | Where we satisfy it | Evidence type | Status |
|---|------------------------|---------------------|---------------|--------|
| A1 | "agents with clear decision logic tied to real signals" | GuardAccount's `_classify` computes every tier deterministically from **on-chain signals**: allowlist membership, recipient first-seen block, UTC hour, per-tx/daily caps, velocity. No LLM in any decision path. `contracts/src/GuardAccount.sol`; engine mirror `packages/engine/src/engine.ts` (30 tests incl. same-inputs-same-verdict). Live proof: the $900 → ATTACKER payment tripped `TIER_VIOLATION/OVER_PER_TX` and was **blocked pre-broadcast** (no tx hash by design — ATTACKER balance 0, confirmed in report §3); the $150 → 3-day-old FRESH_WALLET tripped `NEW_RECIPIENT` → hold (tx `0xf795bf3f813248f4fd463adb91af6ca12c4208a51137de423f5b4d5e440934a1`). | Live chain txs + contracts + tests | **DONE** |
| A2 | "autonomous spending/payments/settlement in USDC" | The REPAYD guard agent **autonomously executed four USDC payroll payments** on Arc testnet from its own agent key (`0x5c53855E30CfbB4e4a6341Bba6129a73cb450F9f`): $180→ALICE (`0xecd4321f…`), $310→BOB (`0x84beb66a…`), $90→ALICE (`0x9aff8383…`), $220→BOB (`0x52df9932…`) — all to guard `0xB30553e2f132126B951D3a6AD4E07EbAa5523b6E`, USDC `0xb95fe4d7EEDb98693ded8F3c1a34A59F6Dac114B`. Autonomous **settlement**: covered-claim payout $135 (135000000 units) to the policy owner in the **same block as the verdict** (tx `0xed020f97235164333e416ae494c6b24462a6884e1aa2ba1a1898b85d07a299f6`, `VerdictAccepted(digest 0x1af0…d3f0, payout=135000000)`; Amara balance = exactly 135000000, was 0 before run). | Live chain txs (Arcscan-linked in report §1) | **DONE** |
| A3 | "use of Agent Stack to connect agents to wallets, USDC payments, onchain actions" | **LIVE — authenticated Circle reads, independently re-verified by Main**: `packages/api/src/circle/agent-wallet.ts` hits Circle Developer-Controlled Wallets on the production base `https://api.circle.com/v1/w3s/*` (key auto-normalized to the 3-part `TEST_API_KEY:` form — the 404s were the wrong API path, not the key). `GET /v1/circle/agent-wallet` returns `{"mode":"live", entityAppId:"defb02cd-…", walletSetCount, walletCount, gatewayDomains:12 (Arc = domain 26)}` side-by-side with the GuardAccount's on-chain USDC (3200): live reads of entity config, wallet sets, wallets, and the **Circle Gateway unified balance** (commits `c90cd66`/`f7688ca`, 11 tests). Agent↔wallet binding also exists on-chain: `identity.getAgentWallet(894341)` = `0x9675…d482`; "onchain actions" = the three ERC-8004 registry posts in the spine. Mutations (wallet-set/wallet create, one gated spend) are coded + endpoint-proven (Circle's 156013 gate reached = path live) and gated on `CIRCLE_ENTITY_SECRET` (C3). Dry-run stays the enforced no-key default (fetch never called, test-pinned). Plan + rejected paths: [`circle-agent-stack.md`](circle-agent-stack.md) §3. | Live 200 JSON + registry readback + tests | **DONE — LIVE (reads)** / mutations gated (C3) |
| A4 | "Nanopayments/Paymaster/App Kits where relevant" | Nanopayments: **live Circle Gateway balance read** (A3 endpoint) + per-2s-block pricing (`packages/pricing`, commit `e4a905c`) — the metering primitive Gateway sub-cent USDC x402 payments settle against; the Hedera rail already executes REAL x402 payments end-to-end (H1/H2: two CRYPTOTRANSFER settlements). Paymaster: Arc gas is USDC-native with no hosted Circle paymaster API — self-operated ERC-4337 evaluated + deliberately rejected (plan §3 Path C). App Kits: three dashboards are the MVP surfaces (A5); embed rejected as duplicative (Path D). "Where relevant" answered with explicit reasons. | Live gateway read + settlements + plan §3 | **DONE** (metering + live reads; deposit→pay step gated C3) |
| A5 | Gate 1: "Functional MVP: working frontend AND backend plus an architecture diagram" | Backend: Coverage API `packages/api/src/server.ts` — `/v1/coverage` (+config-gated on-chain PolicyRegistry bridge, commits `bacaeb5`/`e3d331a`) plus **LIVE chain-read endpoints** `/v1/atlas/overview` + `/v1/circle/agent-wallet` (commit `378978f`, curl-verified against Arc). Frontend: owner dashboard live-wired through serve.ts proxies (commit `4c7958c`, `GET /api/atlas/overview` 200 with real guard/dailyState/hold/pool/payout + ERC-8004 card). Architecture diagram: [`architecture.md`](architecture.md) — full-stack mermaid incl. Circle + Hedera rails, live-proof appendix (commit `2f51de0`). | Code + tests + live curl + diagram | **DONE** |
| A6 | Gate 2: "Video demonstration + presentation: succinctly outlining core functions and effective use of Circle's Developer tools, supported by detailed documentation" | Script ready to record: `docs/submission/video-scripts.md` §ARC — second-by-second beats over the **real** Step-4 run (14 Arcscan tx links), Circle Agent Stack touchpoints at beats ARC-2/ARC-7. Documentation: this matrix + `docs/submission/circle-agent-stack.md`. Recording requires screen-capture only (no new creds). | Script + on-chain evidence | **DONE** (script) / recording pending |
| A7 | Gate 3: "GitHub repo link" | This repo (README rewritten as submission README with track claims + quickstart). | Repo | **DONE** |
| A8 | "State the bounty in the submission" | README headline + this matrix title state the Arc bounty explicitly. | README | **DONE** |
| A9 | ⚑ **RIDER: "$2,500 requires Arc MAINNET deploy by Sept 30"** ($2,500 base + $2,500 mainnet rider at stake) | Contracts are chain-agnostic and deploy via `contracts/script/Deploy.s.sol` (demo-ready, commit `514f5eb`) with per-chain `deployments/<chainId>.json` — the identical flow that produced the verified testnet stack. Mainnet deploy needs: Arc mainnet RPC + funded deployer (mainnet USDC gas) + real USDC addresses + pool seed. **DECIDED (user, 2026-09-12): PURSUE — deploy + re-run the demo on mainnet within the pre–Sept-30 funding window.** Execution is the proven two-command sequence (`forge script … --broadcast` + one demo run); the $5,000 of riders hinges on it. | Decision + deploy script + live testnet precedent | **PENDING-FUNDING** (checklist C4) |

---

## Track 2a — The Graph: "Best AI Tooling or AI Use Case (From Scratch)" ($5,000)

| # | Requirement (verbatim) | Where we satisfy it | Evidence type | Status |
|---|------------------------|---------------------|---------------|--------|
| G1 | "The Graph load-bearing (Subgraphs/Subgraph MCP/Substreams as source of blockchain data)" | The **REPAYD Risk Subgraph** (`packages/subgraph`) is the *single shared read replica* for the whole protocol: the pricing engine and résumé builder both consume it instead of running indexers (`packages/subgraph/README.md` §36 story; `packages/pricing` + `packages/record` read `Agent`/`Verdict`/`Claim`/`BlocklistEntry` entities via `PricingEvent` projection). 38 events across all six fresh-stack contracts, 12 entities, mappings commit `0102a3c`, schema/manifest commit `56920f3`, codegen+build verified (commit `4258268`). | Code + build | **DONE** |
| G2 | "consume LIVE data from a Graph provider (Subgraph Studio API key; 'Mocked, local-only, or static datasets do not qualify')" | **LIVE** — Risk Subgraph `https://api.studio.thegraph.com/query/1760165/repayd-risk-arc/v0.1.3` serves indexed guard and claim data (135000000 payout, covered=true). ERC-8004 registries `https://api.studio.thegraph.com/query/1760165/repayd/v0.1.1` was verified at block **61,875,630**: agent **894341**, validation **25**, responseHash `0x1af0…d3f0` matching the verdict digest, feedback **−2500 @ 2dp**, tags `bulwark-verdict` / `covered`, isRevoked false. | Live endpoint queries | **DONE — LIVE** |
| G3 | "meaningful work with the data (reasoning/decisions/automation/NL interface, not printing raw results)" | The AI/automation consumers: (a) **pricing engine** reasons over subgraph state — streak/claim/anomaly multipliers per §12 (commits `900e260`, `21cebcb`, `68a141f`); (b) **live risk-posture consumer** `packages/api/scripts/query-graph.ts` — EXECUTED LIVE against the deployed endpoint: agent 0xb305…3b6e → multiplier **2.7×**, monthly premium **135 USDC**, labeled reasons (CLAIM_LOAD ×3 from paid claim ts 1789160834; SDK_DISCOUNT −10%), deterministic NL summary, anomaly flag POST_CLAIM_NO_STREAK (capture in graph-deploy.md §LIVE evidence); (c) résumé builder renders INSURED/DRIVING/CLAIMS/ALIBI/BACKING/STATUS with VERIFIED/COMPUTED provenance labels (commit `4f9457b`). | Live run output + tests | **DONE — LIVE** |
| G4 | "open-source + README" | Repo + this README + `packages/subgraph/README.md`. | Repo | **DONE** |
| G5 | "2-4 min video" | `docs/submission/video-scripts.md` §GRAPH (3:30 target): deployed-subgraph live query → AI consumer computes live Risk Posture → pricing decision from the same data. | Script | **DONE** (script) / recording pending G2 |
| G6 | "Start Fresh pool" (we are NOT continuity) | First-time ETHOnline project; subgraph authored from scratch this hackathon (commits `56920f3`→`4258268`, this session). | Git history | **DONE** |

## Track 2b — The Graph: "Best Use of Composable or Standardized Graph Products" ($5,000)

| # | Requirement (verbatim) | Where we satisfy it | Evidence type | Status |
|---|------------------------|---------------------|---------------|--------|
| S1 | "compose 2+ Graph products OR build meaningfully on a standardized schema (e.g. Messari Standardized Subgraphs)" | **Both paths LIVE**: the bespoke Risk Subgraph (`repayd-risk-arc` v0.1.3) and ERC-8004 registry subgraph (`repayd` v0.1.1) expose protocol verdicts and standard-registry mirrors. The composition query joins Step-4 agent **894341** to its validation and feedback: score **25**, feedback **−2500 @ 2dp**, and the matching verdict digest. The registry schema indexes canonical identity, validation, and reputation events. | Two live endpoints + registry readbacks | **DONE — LIVE** |
| S2 | "live provider data" | Both halves LIVE on Studio: `repayd-risk-arc` v0.1.3 and `repayd` v0.1.1. The ERC-8004 manifest starts at **61,614,321** (a bounded history window before Step-4). Verified query at block **61,875,630** returned agent 894341's wallet, validation, and covered feedback. **COST: $0** — Studio free tier. | Live endpoints + indexed data | **DONE — LIVE** |
| S4 | "CONTRIBUTING a standardized subgraph counts as in-scope" | The ERC-8004 registry subgraph (S1a) is LIVE on Studio (`repayd` v0.1.1) and written to be contributed back to the Agent0 ecosystem post-hackathon; the Risk Subgraph README documents the public-good intent (§36 story). | Code + live deployment | **DONE** (live; contribution PR is post-event) |

---

## Track 3 — Hedera: "AI & Agentic Payments on Hedera" ($6,000, up to 3 × $2,000)

| # | Requirement (verbatim) | Where we satisfy it | Evidence type | Status |
|---|------------------------|---------------------|---------------|--------|
| H1 | "host a LIVE x402-gated service on Hedera testnet settled through the Blocky402 facilitator" | `packages/hedera` — the REPAYD Coverage & Risk API with three pay-per-call endpoints behind an x402 `exact`/`hedera:testnet` paywall, verification + settlement delegated to the hosted Blocky402 testnet facilitator `https://api.testnet.blocky402.com`. **LIVE**: service running on :4601 (payTo 0.0.10484593); `GET /v1/x402/quote` returns 402 with PAYMENT-REQUIRED `{scheme:exact, network:hedera:testnet, amount:"100000", asset:"0.0.0", payTo:"0.0.10484593", extra:{feePayer:"0.0.7162784"}}`; `/healthz` meter reports `quote:1` served / 100000 billed. Design: [`hedera-x402-plan.md`](hedera-x402-plan.md) §1–§3; evidence: [`packages/hedera/README.md`](../../packages/hedera/README.md) §Demo. | Live service + captured challenge | **DONE — LIVE** |
| H2 | "build a platform/agent that consumes it and completes at least one REAL paid request end-to-end" | `packages/hedera/scripts/paid-request.ts` — the REPAYD payer agent discovers via `/v1/x402/services`, pays the 402 challenge through Blocky402 (payer-signed `TransferTransaction`, facilitator fee-payer), receives the 200 payload, prints the settlement tx + HashScan link. **LIVE ×2, both CRYPTOTRANSFER SUCCESS (independently re-queried on the Hedera mirror node)**: settlement `0.0.7162784-1789189943-758655274` and `0.0.7162784-1789190197-536086930` (payer −100000 / payTo +100000, facilitator paid 268377 fee); served payload = live quote multiplier 2.1958 / 109792800 monthly, identity cross-verified vs the canonical Arc ERC-8004 registry. | Real settlement txs (mirror-node verified) | **DONE — LIVE** |
| H3 | "public repo + README (setup/architecture/payment flow)" | [`packages/hedera/README.md`](../../packages/hedera/README.md) — setup, architecture, payment flow, extra-points matrix, and §Demo LIVE EVIDENCE with the two settlement tx ids + links (commit `f914aec`); plan in [`hedera-x402-plan.md`](hedera-x402-plan.md). Public repo: this repository. | README + repo | **DONE** |
| H4 | "demo video ≤5 min showing the paid request executing" | `docs/submission/video-scripts.md` §HEDERA (4:30 target). **Recording-ready**: the service is live on :4601 and every re-run of `paid-request.ts` mints a fresh CRYPTOTRANSFER settlement + HCS memo, so the video can show a brand-new tx id on HashScan. | Script + live repeater | **DONE** (script) — ready to record |
| H5 | Extra: "pay-per-call inference/data/compute metering" | ✅ Per-endpoint pricing (`src/pricing-meter.ts`): quote 0.001 HBAR, verdict record 0.002 HBAR, résumé 0.0015 HBAR — each 402 challenge carries the exact tinybar price, `/v1/x402/services` advertises prices, `/healthz` counts served/billed per endpoint (**live meter after the runs: quote:1 served / 100000 billed**). Mirrors the per-block pricing philosophy of `packages/pricing`. | Live meter output | **DONE — LIVE** |
| H6 | Extra: "on-chain agent identity via ERC-8004 or HCS-14" | Payer identifies as ERC-8004 **agentId 894341**; the service cross-validates via read-only `ownerOf`/`getAgentWallet` eth_calls against the canonical identity registry on Arc — **live-run payload shows `identity.status:"verified"`**, owner `0x0549…33A6`, wallet `0x9675…d482`; the id is stamped into the HCS audit memo. Identity on Arc, payments on Hedera — one cross-chain trust story. | Live served payload + HCS memos | **DONE — LIVE** |
| H7 | Extra: "agent discovery via UCP or a directory" | `GET /v1/x402/services` — JSON discovery directory (endpoint, description, mimeType, price, network, scheme, payTo) consumed by the payer agent before paying (live HTTP 200 on the running service). Plus ENSv2 résumé discovery: **`atlasrepayd.eth` registered LIVE on Sepolia** (resolver `0x2a63444c…6dac`, 8 text records read back via UniversalResolver; txs in the evidence spine; commits `793f0e9`–`0ac0c2f`). | Live directory + Sepolia txs + resolve | **DONE — LIVE** |
| H8 | Extra: "HTS tokens or custom fee schedules in settlement" | Native-HBAR (`asset:"0.0.0"`) settlement PROVEN live (both CRYPTOTRANSFERs); per-endpoint tinybar table is the fee schedule (H5). HTS settlement (testnet USDC `0.0.429274`) is a documented config flip via the same `ExactHederaScheme.asset` (needs token association on both accounts) — not defaulted. | Live settlement + plan §4 | **DONE** (HTS flip documented) |
| H9 | Extra: "verifiable payment audit trails on HCS" | **LIVE**: topic `0.0.10493275` created on testnet, 2 receipt memos submitted after settlements (`{kind:"x402-receipt", endpoint, price, agentId 894341, identityVerified:true}`), publicly verifiable → [hashscan.io/testnet/topic/0.0.10493275](https://hashscan.io/testnet/topic/0.0.10493275). | HCS topic txs | **DONE — LIVE** |
| H10 | Extra: "recurring/streamed payments via Scheduled Transactions" | **LIVE**: `scripts/schedule-premium.ts` created AND executed schedule `0.0.10493353` (executed_timestamp 1789190167.858871887, → [hashscan](https://hashscan.io/testnet/schedule/0.0.10493353)); README documents the per-block premium → ScheduleCreate recurring-draw mapping. | Live schedule tx | **DONE — LIVE** |

---

## The evidence spine (what every "live chain" claim above traces to)

- **Step-4 run (Arc testnet 5042002, 2026-09-11, blocks 61625041–61625840):** 14 tx hashes, every one re-fetched independently (status 0x1, correct `to`, correct sender) — full table with Arcscan links in [`local/step4-final-report.md`](../../local/step4-final-report.md) §1.
- **ERC-8004 mirror (readback from canonical registries):** agentId **894341**, owner `0x05499b0be3B9E9Db3Cc5124b2F682513D94133A6` (ops), wallet binding `0x9675b4D20d2ACFE55D00a02D55B9cdb57AEbD482`; validation score **25 (COVERED)**, responseHash `0x1af03bc6a70b6309bd5c9ec92c7d78c1024e0d69c9ea5ea60faf828c958ed3f0` (== on-chain `VerdictAccepted.digest`), tag `bulwark-verdict`; feedback value **−2500 @ 2dp**, tags `bulwark-verdict`/`covered`, not revoked.
- **Fresh-stack addresses:** `contracts/deployments/5042002.json` — guard `0xB30553e2…3b6E`, pool `0x35d78e52…72e4`, policyRegistry `0xcC34D028…748a`, verdicts `0x7B46f82B…C52f`, blocklist `0x19afa081…8322`, USDC `0xb95fe4d7…114B`.
- **Key commits:** ERC-8004 orchestrator `db98c7f`/`cad581a`; subgraph schema+mappings `56920f3`/`0102a3c`/`4258268`; pricing `e4a905c`/`900e260`/`21cebcb`; record/ENSv2 `4f9457b`/`7657223`/`62b9c88`; API bridge `bacaeb5`/`e3d331a`; deploy script `514f5eb`.
- **Graph deploy evidence:** [`docs/submission/graph-deploy.md`](graph-deploy.md) — verified step-by-step Studio deploy procedure (`graph auth` + IPFS upload confirmed live), COST $0 breakdown (100k queries/mo free tier, no card, no GRT), endpoint URL format, and the two-subgraph composition query pattern.
- **Hedera x402 settlements (testnet, 2026-09-12):** CRYPTOTRANSFER `0.0.7162784-1789189943-758655274` + `0.0.7162784-1789190197-536086930`, both SUCCESS (mirror node re-queried: payer `0.0.10484477` −100000, payTo `0.0.10484593` +100000, facilitator `0.0.7162784` paid 268377 fee); HCS audit topic `0.0.10493275` (2 memos, agentId 894341, identityVerified true); Scheduled Transaction `0.0.10493353` created AND executed. Full captures in [`packages/hedera/README.md`](../../packages/hedera/README.md) §Demo.
- **ENSv2 registration (Sepolia, 2026-09-12, `atlasrepayd.eth`):** resolver-proxy deploy `0xaf8834d676da9fb23c720d8ee82a4ad9599c35117fa277b47b7e047be6ab8d4f` → MockUSDC approve `0x11c599b908fa9397d6ad5989f848506c56f8c9f297aec01a4672addad35648a5` → commit `0x0d9fc5d0c50564295202afd419d9d3097e68e61a87681b95e38c23ed86a6e6c5` → register `0x4a3ca0e495d1f879d21be08f3c44eac68312eaec05e1c5181b9d7f70fb64ef2f`; `setResolver` repair `0xd336ba893d32cdf2db3bd7f0cb1d4bbb7c5d47e5f86ea08102df244e1a939051`. Resolver proxy `0x2a63444cd961e4284e60b0314f58c707a8c86dAC`, tokenId `40131516475764134121536160922125412142801822368892093348170664971358229757952`; `bun run scripts/resolve.ts atlasrepayd.eth` reads back `com.bulwark.resume` + all 6 per-field records from the UniversalResolverProxy (owner `0xF1F70e591f9674DF7051e9EA892dDA00D3238384`). Re-runs of `scripts/register.ts` are zero-tx idempotent.