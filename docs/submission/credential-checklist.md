# REPAYD — Credential Checklist (priority order)

Every `[NEEDS:*]` item across the submission docs, with owner and what it unblocks. Priority = first-place impact per hour of waiting: the Graph deploy key alone unblocks **two** $5,000 tracks' live-data gates; Hedera unblocks the $6,000 track's core requirement; Circle is an enhancement on a track that is already largely DONE; ENSv2 is a discovery extra-credit; the Arc mainnet decision gates $2,500.

**Cost summary: every credential is $0 except one.** C1 Graph = $0 (Studio free tier, 100k queries/mo, no card, no GRT); C2 Hedera = $0 (portal faucet); C3 Circle = $0 (sandbox/testnet); C5 Sepolia = $0 (faucets). **C4 Arc mainnet is the only real-money item** (deployer gas + real USDC for the re-run demo) — user is pursuing mainnet, funding deferred to the pre-Sept-30 window.

| Pri | Credential / decision | Owner | Unblocks | Tracked as | Status |
|-----|----------------------|-------|----------|-----------|--------|
| **C1** | **The Graph Subgraph Studio deploy key** (create subgraph at thegraph.com/studio, e.g. "Repayd Risk Arc Testnet", grab deploy key) | GraphDeploy agent → needs key from Main/user | `graph auth` + `graph deploy` of the Risk Subgraph to `arc-testnet`; unblocks both Graph tracks' live-provider-data gates (G2/S2) | `[NEEDS:GRAPH_DEPLOY_KEY]` → **CLEARED** | **DONE — LIVE**: Risk Subgraph `repayd-risk-arc` v0.1.3 + ERC-8004 registries `repayd` v0.1.1; Step-4 agent 894341's validation and feedback queried at block 61,875,630. Live Risk Posture consumer: 2.7× multiplier / 135 USDC. **Cost: $0** — Studio free tier. |
| **C2** | **Hedera testnet accounts** (2× ECDSA via portal.hedera.com, faucet-funded: operator/payer `HEDERA_OPERATOR_ID`+`_KEY`, service/payTo `HEDERA_SERVICE_ID`+`_KEY`; no Blocky402 key needed — hosted testnet facilitator is open access) | HederaX402/HederaLiveRun | Bring `packages/hedera` service live; execute the first REAL paid request end-to-end (`scripts/paid-request.ts`); HCS audit topic; `schedule-premium.ts` recurring settlement; **unblocks the Hedera track's core requirements (H1–H4) and video (HEDERA-2..9)** | `[NEEDS:HEDERA_TESTNET_KEYS]` → **CLEARED** | **DONE — LIVE**: two real paid x402 requests settled (CRYPTOTRANSFER SUCCESS on mirror node), HCS topic `0.0.10493275` carrying audit memos, Scheduled Transaction `0.0.10493353` created AND executed, service hot on :4601 for the video. **Cost: $0** — testnet faucet. |
| **C3** | **Circle API key + entity secret** (`CIRCLE_API_KEY` + `CIRCLE_ENTITY_SECRET` from console.circle.com; optional `CIRCLE_WALLET_SET_ID`/`CIRCLE_AGENT_WALLET_ADDRESS` to reuse; `CIRCLE_DEMO_SPEND=1` for the single spend) | CircleLiveRead | Live authenticated reads on `api.circle.com/v1/w3s/*` (matrix A3) — DONE; wallet-set/wallet create + one gated USDC spend + Gateway deposit→x402 pay (A4) need `CIRCLE_ENTITY_SECRET` | `[NEEDS:CIRCLE_API_KEY]` → **KEY VALID — READS LIVE** | **DONE — LIVE (reads)**: key works in the 3-part form (`TEST_API_KEY:` + the console's 2-part value, auto-normalized); `/v1/circle/agent-wallet` serves real `entityAppId` + wallet-set/wallet + Gateway unified-balance reads (12 domains, Arc=26). **Entity-secret caveat:** registration is one-shot; a provisioning probe consumed it before persistence (recovery file in `local/circle-recovery.dat`, uncommitted). To unlock create/spend: reset the entity secret at console.circle.com/wallets/dev/configurator/entity-secret using that recovery file, then set `CIRCLE_ENTITY_SECRET=<64-hex>` — the code path is already endpoint-proven (156013 gate). **Cost: $0.** |
| **C4** | **Arc MAINNET deploy** — fund a mainnet deployer, run `Deploy.s.sol` against Arc mainnet, re-run demo on mainnet stack | Main (user) + Step4-style run | The **$2,500 mainnet rider** of each Arc bounty ($2,500 + $2,500 across the two Arc tracks, deploy by **Sept 30, 2026**). Contracts + deploy script are chain-agnostic and proven (the identical flow produced the verified testnet stack, commit `514f5eb`) | `[NEEDS:ARC_MAINNET_DECISION]` → **DECIDED: PURSUE (user, 2026-09-12)** | **PENDING-FUNDING** — user funds the mainnet deployer (USDC gas + real USDC for guard backing/pool seed) in the pre–Sept-30 window; I execute deploy + demo re-run the moment keys land. The only real-money item on this checklist. Matrix row A9. |
| **C5** | **Sepolia ETH + MockUSDC** (faucet ETH for gas; MockUSDC for the record package's gated scripts) | ResumeBuilder/record work | Register the ENSv2 name (e.g. `atlas.repayd.eth`) via commit-reveal + PermissionedResolver, resolve the public résumé; unblocks Hedera extra-credit "agent discovery via UCP or a directory" (H7) and the ENSv2 first-mover story | `[NEEDS:SEPOLIA_FUNDS]` → **CLEARED** | **DONE — LIVE-VERIFIED on Sepolia (`atlasrepayd.eth`, owner `0xF1F70e591f9674DF7051e9EA892dDA00D3238384`)**. Resolver proxy `0x2a63444cd961e4284e60b0314f58c707a8c86dAC`; tokenId `40131516475764134121536160922125412142801822368892093348170664971358229757952`. Tx flow: deployProxy `0xaf8834d676da9fb23c720d8ee82a4ad9599c35117fa277b47b7e047be6ab8d4f` → approve `0x11c599b908fa9397d6ad5989f848506c56f8c9f297aec01a4672addad35648a5` → commit `0x0d9fc5d0c50564295202afd419d9d3097e68e61a87681b95e38c23ed86a6e6c5` → register `0x4a3ca0e495d1f879d21be08f3c44eac68312eaec05e1c5181b9d7f70fb64ef2f` (+ `setResolver` repair `0xd336ba893d32cdf2db3bd7f0cb1d4bbb7c5d47e5f86ea08102df244e1a939051` — see commits `9fcc00b`/`aa22608`). `bun run scripts/resolve.ts atlasrepayd.eth` reads back `com.bulwark.resume` + all 6 per-field records from the UniversalResolverProxy. **Cost: $0** — both faucets used. |
| **C6** | (implicit) Arcscan/HashScan are public; no keys needed for verification reads | — | None — all evidence readbacks in the matrix are public RPC + explorer | — | N/A |

### Remaining actions (as of 2026-09-12)
1. **Videos** (all three tracks): scripts ready; services LIVE for recording — Hedera x402 on :4601 (fresh settlement per run), API+dashboard on :8787/:3000, Graph endpoints public. ARC needs no credentials.
2. **C4 Arc mainnet deploy** (only real-money item): funding + `forge script --broadcast` + one demo re-run, before Sept 30 → unlocks $5,000 of riders.
3. **C3 entity-secret reset** (console, recovery file) → wallet create + one demo USDC spend on the Arc Circle track (A3/A4 mutation cells).
4. Optional: `repayd-erc8004` gets its own Studio slug name later (currently deployed under `repayd`).

### Where these appear in the docs
- `docs/submission/compliance-matrix.md` — rows G2/S2 (C1), H1–H4 (C2), A3/A4 (C3), A9 (C4), H7 (C5).
- `docs/submission/video-scripts.md` — all three scripts record-ready NOW (C1/C2 cleared, Circle reads live; a fresh x402 settlement is minted per HEDERA take).
- `README.md` — "What needs credentials" section.
