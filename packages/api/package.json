{
  "name": "@repayd/api",
  "version": "0.1.0",
  "description": "REPAYD Coverage API — embedded insurance for agent platforms. POST /v1/coverage.",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch src/server.ts",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@repayd/agent-sdk": "workspace:*",
    "@repayd/engine": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
