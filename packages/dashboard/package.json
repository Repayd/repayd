{
  "name": "@repayd/dashboard",
  "version": "0.1.0",
  "description": "REPAYD dashboards — Owner (policy, alerts, driving record), Capital (pool, tranches, claims feed), Public record lookup.",
  "type": "module",
  "scripts": {
    "start": "bun run serve.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
