{
  "name": "@repayd/demo",
  "version": "0.1.0",
  "description": "REPAYD demo — Atlas, the attack that never settles, and the same-block payout. Runs against any deployed stack (anvil or public testnet).",
  "type": "module",
  "scripts": {
    "demo": "bun run src/demo.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@repayd/agent-sdk": "workspace:*",
    "@repayd/api": "workspace:*",
    "@repayd/engine": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
